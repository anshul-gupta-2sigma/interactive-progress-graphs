,snapshot,timestamp,real_progress,reg_est_prog,feedback_text,code
0,1,,0.0,0.0129,, 
1,2,2019-10-30 14:00:35,0.8699421496765013,0.8999,success,"
import string

# the list of special words - you can use this as a global variable
# within your function
specialWords = ['a', 'an', 'the', 'at', 'by', 'for', 'in',
           'of', 'on', 'to', 'up', 'and', 'as',
           'but', 'or', 'nor']

# define the capTitle() function here
def capTitle(line):
    titleCap = ''
    titleList = line.split(' ')
    
    for i in range(len(titleList)):
        for j in range(len(specialWords) - 1):
            if(titleList[i] == specialWords[j]):
                break
            elif( j == len(titleList) - 1):
                titleList[i].capitalize()
        titleCap = titleCap + titleList[i]
    return titleCap


# DO NOT CHANGE CODE BELOW

print(capTitle(input(""Enter title: "")))

"
2,3,2019-10-30 14:43:49,0.8913656170470036,0.8854,success,"
import string

# the list of special words - you can use this as a global variable
# within your function
specialWords = ['a', 'an', 'the', 'at', 'by', 'for', 'in',
           'of', 'on', 'to', 'up', 'and', 'as',
           'but', 'or', 'nor']

# define the capTitle() function here
def capTitle(line):
    titleCap = ''
    titleList = line.split(' ')
    
    for i in range(len(titleList)):
        for j in range(len(specialWords) - 1):
            if(titleList[i] == specialWords[j]):
                break
            elif( j == len(titleList) - 1):
                titleList[i].capitalize()
        titleCap = titleCap + ' ' + titleList[i]
    return titleCap


# DO NOT CHANGE CODE BELOW

print(capTitle(input(""Enter title: "")))

"
3,4,2019-10-30 14:55:08,0.8796064230142315,0.8867,success,"
import string

# the list of special words - you can use this as a global variable
# within your function
specialWords = ['a', 'an', 'the', 'at', 'by', 'for', 'in',
           'of', 'on', 'to', 'up', 'and', 'as',
           'but', 'or', 'nor']

# define the capTitle() function here
def capTitle(line):
    titleCap = ''
    titleList = line.split(' ')
    
    for i in range(len(titleList)):
        for j in range(len(specialWords)):
            if(titleList[i] == specialWords[j]):
                break
            elif( j == len(specialWords) - 1):
                titleList[i].capitalize()
        titleCap = titleCap + ' ' + titleList[i]
    return titleCap


# DO NOT CHANGE CODE BELOW

print(capTitle(input(""Enter title: "")))

"
4,5,2019-10-30 15:09:23,0.93522973760593,0.8512,success,"
import string

# the list of special words - you can use this as a global variable
# within your function
specialWords = ['a', 'an', 'the', 'at', 'by', 'for', 'in',
           'of', 'on', 'to', 'up', 'and', 'as',
           'but', 'or', 'nor']

# define the capTitle() function here
def capTitle(line):
    titleCap = ''
    titleList = line.split(' ')
    
    for i in range(len(titleList)):
        for j in range(len(specialWords)):
            if(titleList[i] == specialWords[j]):
                titleCap = titleCap + ' ' + titleList[i]
                break
            elif( j == len(specialWords) - 1):
                titleCap = titleCap + ' ' + titleList[i].capitalize()
    return titleCap


# DO NOT CHANGE CODE BELOW

print(capTitle(input(""Enter title: "")))

"
5,6,2019-10-30 15:16:36,0.9918629028815845,0.7837,NameError: name 'titlelist' is not defined on line 13,"
import string

# the list of special words - you can use this as a global variable
# within your function
specialWords = ['a', 'an', 'the', 'at', 'by', 'for', 'in',
           'of', 'on', 'to', 'up', 'and', 'as',
           'but', 'or', 'nor']

# define the capTitle() function here
def capTitle(line):
    titleList = line.split(' ')
    titleCap = titlelist[0].capitalize()
    
    for i in range(1, len(titleList)):
        for j in range(len(specialWords)):
            if(titleList[i] == specialWords[j]):
                titleCap = titleCap + ' ' + titleList[i]
                break
            elif( j == len(specialWords) - 1):
                titleCap = titleCap + ' ' + titleList[i].capitalize()
    return titleCap


# DO NOT CHANGE CODE BELOW

print(capTitle(input(""Enter title: "")))

"
6,7,2019-10-30 15:17:04,0.9999999507990761,0.7773,success,"
import string

# the list of special words - you can use this as a global variable
# within your function
specialWords = ['a', 'an', 'the', 'at', 'by', 'for', 'in',
           'of', 'on', 'to', 'up', 'and', 'as',
           'but', 'or', 'nor']

# define the capTitle() function here
def capTitle(line):
    titleList = line.split(' ')
    titleCap = titleList[0].capitalize()
    
    for i in range(1, len(titleList)):
        for j in range(len(specialWords)):
            if(titleList[i] == specialWords[j]):
                titleCap = titleCap + ' ' + titleList[i]
                break
            elif( j == len(specialWords) - 1):
                titleCap = titleCap + ' ' + titleList[i].capitalize()
    return titleCap


# DO NOT CHANGE CODE BELOW

print(capTitle(input(""Enter title: "")))

"
